"""shopsystem-decisions — the progressive-disclosure decision system.

A single-source frontmatter schema, a deterministic L0/L1/index generator, and a
coherence gate (FC1-FC4) behind one ``decisions`` CLI. Stdlib + PyYAML only;
network- and model-free by construction.
"""

__version__ = "1.0.0"
