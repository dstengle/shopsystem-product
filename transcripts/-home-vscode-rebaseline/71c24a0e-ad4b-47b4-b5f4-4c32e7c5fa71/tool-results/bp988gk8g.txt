=== glossary: run, step-related terms ===
34:- **step** — one unit of a process: an agent step (carries a prompt) or a
104:- **run** — one execution of a process, anchored to a work item; states:

=== 'agent run' occurrences across the repo ===
basis/README.md:73:  agent runs it through. `tools/descriptions/` is the home the
basis/artifacts/run-cost.md:19:  per agent run recorded on the session's anchor, naming step, role,
basis/artifacts/run-cost.md:38:1. **Rows** — one table row per agent run the anchor records: step,
basis/processes/skill-rendering.md:32:of that activity and an agent running a tool operates from what the
basis/processes/session-handoff.md:37:  a cost row for each agent run that item recorded, beside the session
basis/processes/session-handoff.md:206:| O5 | a cost row per agent run on `run_anchor`, none computed by a model, no row for a runtime step | mechanical | `write-cost-rows.run` |
features/feat-roles-availability.md:22:  so that no role the agent runtime instantiates comes from an
features/feat-roles-availability.md:37:- *a role available to the agent runtime that traces to no approved definition is reported* — shopsystem-product (the lead shop)
features/feat-roles-availability.md:41:No usability acceptance criteria and no accessibility criteria are due for these scenarios: the initiative's For whom section names no interaction type — the outcome is consumed inside the agent runtime's instantiation of the role, no core task on the core-task list carries it, and the rendered role is outside `agent-is-a-user`'s closed set of agent-facing interfaces; no failure or boundary case follows for the Edges table. (designer, 2026-09-03)
features/feat-roles-availability.md:47:None — the outcome is consumed inside the agent runtime's
features/feat-roles-availability.md:59:  so that no role the agent runtime instantiates comes from an
features/feat-roles-availability.md:67:    Then that role is available to the agent runtime from the approved source, current with its approved definition
features/feat-roles-availability.md:72:    When the agent runtime instantiates the role for that step
features/feat-roles-availability.md:73:    Then the role the agent runtime instantiates for that step is loaded from the approved source and is current with the approved definition of lead-po
features/feat-roles-availability.md:79:    Then no role from that definition is available to the agent runtime
features/feat-roles-availability.md:89:    Given an approved role definition with no role available to the agent runtime
features/feat-roles-availability.md:91:    Then the check reports that role as not available to the agent runtime
features/feat-roles-availability.md:94:  Scenario: a role available to the agent runtime that traces to no approved definition is reported
features/feat-roles-availability.md:95:    Given a role available to the agent runtime that no approved role definition is the source of
features/feat-roles-availability.md:103:    Then the role the agent runtime instantiates is current with its approved definition
features/feat-roles-availability.md:107:    Given every approved role definition with a role available to the agent runtime, current with it, and no other role available to the agent runtime
features/feat-roles-availability.md:116:| An approved role definition the agent runtime cannot instantiate | the framing ("the shop's approved role definitions cannot be instantiated by the agent runtime") | Scenario: an approved role definition with no available role is reported; Scenario: an approved role definition is available at the point of work — the framed remedy |
features/feat-roles-availability.md:117:| A role the runtime instantiates that comes from the frozen corpus, unapproved on this branch | the framing ("what it instantiates instead comes from the frozen corpus"); the For whom section ("the two roles the runtime does instantiate come from the frozen corpus, unapproved on this branch") | Scenario: an agent fills a role from the approved source at the point of work — the framed remedy: the runtime loads the role from the approved source, so the corpus is not what it instantiates from; and Scenario: a role available to the agent runtime that traces to no approved definition is reported — for a stray role standing at this branch's load point; the corpus's own load, outside this branch, is out of reach by the Appetite's third no-go |
features/feat-roles-availability.md:135:| 3 | 2026-09-03 | review | PO output check round 2 (judge: claude-fable-5-1 / screen prompt v6): findings — scenario 8's Given did not set up its Then's third clause (confident; Given extended with "and no other role available to the agent runtime", new hash); Edges row 5 still enumerated causes while scenario 7's Given says "whatever the cause" (wobbly; row reworded to the scenario's words); the Feature narrative repeated in the Gherkin block (wobbly). Repaired; the narrative duplication ruled typedef-mandated by the PM role. |
features/feat-roles-availability.md:138:| 5 | 2026-09-03 | state | `checked` → `assigned`: the scenario-assignment record step. One assignment entry — context shopsystem-product (the lead shop), scenarios @hash:ce98da2b6467, @hash:cb8d69b7d24e, @hash:c69e5a0eef5d, @hash:1df06aa89e36, @hash:e0d4e526c948, @hash:9d04a8d0a3b9, @hash:d707d4311bdf, @hash:219547cc8cb5. Pre-state read: the initiative's Decomposition ruling — no Bounded Context touched; the source definitions, the render, and the load point all in the lead shop's tree, the load point `.claude/agents/` absent on this branch; the owning shop matches the Contributors section for every scenario; the feature repository swept in full — two artifacts, this feature and feat-skills-availability, no conflict: the two features specify checks over different definition kinds (process definitions rendered to `.claude/skills/` by the skill-rendering process; role definitions made available to the agent runtime by the process maintaining their approved source), feat-skills-availability's Edges place role definitions out of its scope, and the stray-role scenario (@hash:9d04a8d0a3b9) and the clean pass's third clause have no counterpart there to contradict — a stricter clean condition for a different kind at a different load point; no BC contract in the pre-state — the decomposition names no context whose contract bears on these behaviors. Sent: none — the owning shop is the lead shop itself; the freeze bars dispatch and no Bounded Context exists to receive; the gap stands as lead-ki66p. |
basis/tools/write_cost_rows.py:6:anchor — a `bd` work item — and writes one row per agent run beside
basis/tools/write_cost_rows.py:304:        "Cost rows for the agent runs recorded on the anchor above, read "
initiatives/init-roles-availability.md:24:by the agent runtime; what it instantiates instead comes from the
initiatives/init-roles-availability.md:33:Measure: approved roles the agent runtime instantiates from an
initiatives/init-roles-availability.md:90:| 6 | 2026-09-03 | update | Measure met in the running system: 6 of 6 approved roles instantiated by the agent runtime from the load point, current with their definitions (role-rendering first run, check clean round 2; the instantiation recorded in feat-roles-availability v6). The `completed` state is a pending reconcile-side amendment; the PO role judges the features done. |
features/feat-tool-skills-rest.md:82:where the agent runtime loads skills from; **the check over the load
features/feat-flow-simplification.md:18:  The authority, who frames and reads sweeps, and every role, which makes and self-checks, can move a bet to a verified build in six agent runs or fewer, with no human step past framing and no check inside the flow, in place of twenty runs and six human decisions.
features/feat-flow-simplification.md:41:  The authority, who frames and reads sweeps, and every role, which makes and self-checks, can move a bet to a verified build in six agent runs or fewer, with no human step past framing and no check inside the flow, in place of twenty runs and six human decisions.
features/feat-flow-simplification.md:47:    Then the run passes through six agent runs or fewer
features/feat-flow-simplification.md:96:| Twenty agent runs and six human decisions between bet and build | the framing (Problem) | Scenario: a run goes from bet to verified build in six runs or fewer |
features/feat-process-runner.md:75:the agent runtime loads roles and skills from (feat-roles-availability,
features/feat-process-runner.md:345:    Then the router is available to the agent runtime, current with its definition, and the check reports it so
features/feat-tool-skills.md:92:- **the agent's load point** — where the agent runtime loads skills
initiatives/init-tool-skills.md:52:agent running the lint through the skill. No-gos, each with its
initiatives/init-tool-skills.md:100:| 5 | 2026-09-07 | update | Usability attached by the lead-product-designer role at initiative-check (v9)'s attach-usability step, from its one-sentence prompt. The full offer, in the role-offer type's parts, the 500-word rule keeping it out of §4 (§1–3 stand at 432 words; §4 at 62 before this attachment): *role* — lead-product-designer. *verdict* — none due, with a hypothesis stated regardless. Reasons: §2 names no interaction type, and its reason holds — the core-task list (v4) carries no task a tool skill completes, so `core-task-parity` has no hold, and the initiative typedef's §4 asks this role's evidence only where §2 names a type; by this role's Decisions owned the verdict due is none. Stated regardless because the outcome delivers two interfaces an agent uses, which `agent-is-a-user` bullet 1 makes interfaces designed for their caller and screened by this role whether or not §2 names a type: the skill — read by an agent with nothing else, the api guideline's own voice principle; that guideline covers the agent tool-definition interaction, type `api` — and the standard flag with its answer, a `cli` interaction on the lint under the cli guideline, clig.dev its platform guideline. Observed use: none — no tool skill exists; the one observed use of the tools is the solutions architect role's run of each with `--help` (2026-09-07, this history's v3; the discovery's evidence table, sess-2026-09-06-c), not re-run by this role, which had no shell at this step: the lint answers `--help` by running its normal function and exiting 0, compile_process reads it as a file path, compile_principles exits on a traceback, two print a usage sheet — against the corpus, the help pattern (patterns record; cli guideline rule 3) unmet on three of five tools and the error pattern (common guideline rule 3; cli rule 4) unmet by the traceback: an agent that asks a tool for help today is misled, the failure `tools-through-skills`'s rationale describes. Recommended to the PM role as bearing on value, not as a change to the measure: the current condition is worse than "0 of 11" reads — three tools mislead a caller who asks. The hypothesis, labeled so under `evidence-not-opinion` bullet 1: an agent in a fresh context, given the lint's skill and nothing else, completes each use the skill states on its first invocation and reads each failure as the skill states it. The evidence that replaces it: the proof's last part ("an agent running the lint through the skill") run as measured task completion — the corpus's admissible form — a fresh-context agent with the skill as its only source, the definitions' three uses (bare over the basis; `--derive-chain <artifact_type>`; `--process <path>`) and one failing run, the invocations and results recorded in the feature's delivery; until that run the target of 1 is met as a hypothesis, and the delivery says so (the common guideline's implication on the delivering shop). Usability acceptance criteria for the PO role, one scenario each, with what invalidates it: (a) the agent's first invocation of each use is the one the skill states — invalidated by any read of the tool's source or help, or a second attempt; (b) each use entry states what it does, what it takes with each input named for the caller and omitted inputs' treatment, what it returns, how it fails with a stable code beside an explanation and a next step, and the exact invocation — invalidated by a failure the agent meets that the skill does not name; (c) the skill's `description` names the tool and its uses so the harness offers it for the task — invalidated by the agent proceeding to a bare invocation with the skill present. *decisions* — entries: D1, decision: "A tool skill's use entry takes one shape per use — what it does; what it takes, each input named for the caller, with omitted inputs' treatment; what it returns; how it fails, each failure a stable code beside its explanation and next step; the exact invocation — the api guideline's rules 1–3 applied to a tool skill, entered in the corpus's patterns record for the `api` type as a hypothesis until an agent's use is observed"; record — none: this role's decisions land as entries in the experience guidance corpus, its exclusive domain, not as records in decisions/; the entry is this attachment's resulting action, made before the feature's delivery is screened, since the patterns record (v2) holds no entry for a tool skill and the conformance screen's scenario 2 would return "record lacks the entry" — a finding against the corpus. D2, a part of the solutions architect role's D1 — the experience decisions inside the flag and its answer's shape, this role's as "parts of decisions the authority takes": (i) the flag's name and the shape's field names are names for the caller — a caller says what each is from the name and the vocabulary alone (api rule 1) — entered in the vocabulary with the cli mapping; (ii) the failure part carries a stable code from a closed, documented set beside an explanation and a next step (api rule 3 — the type requires the code, a tightening the principle allows); (iii) the answer goes to standard output with exit 0 and no other action, before anything else the tool would do — clig.dev's help rule, which cli rule 3 derives from and which the lint's `--help` departs from today; (iv) this role screens the field names before the ADR is checked (the role-offer type's own history carries the same recommendation for its fields, pending); record: adr-2026-09-07-tool-answer — carried into the ADR the check's route authored on the architect's D1, whose author reads this history as the subject's evidence; the ADR's id is written in the architect's entry, not here. *risks* — R1: the measure counts a skill standing, and "usable" is a usability claim — without the run in the verdict's reasons the target of 1 is met as a hypothesis, which `evidence-not-opinion` requires be labeled; the PO's scenario (a) is the consumer. R2: the answer is written by the tool's implementer, and a rendering carries its names verbatim — `--derive-chain <artifact_type>` says which chain, derived from what, only to someone who knows the lint; the conformance screen's scenario 1 then returns undecidable, since the vocabulary (v3) lacks every tool term and every entry is a hypothesis — a finding against the corpus, not the skill, so the skill reaches delivery with its names unscreened; mitigation: the vocabulary gains the tool terms (framework tool, skill, use, gap, and the ADR's names) as hypotheses before the screen — this role's action — and D2(i). R3: the harness activates a skill on its `description` (the rendered initiative-check skill: purpose and use-when in one field); a tool skill whose description is the answer's description alone, or a long one, is not offered for the task, and the agent falls back to the bare invocation `tools-through-skills` bullet 2 forbids — the architect's U2 from the caller's side; the PO's scenario (c). R4: three tools mislead a caller who asks for help today; the flag adds a second door beside the broken one, and an agent trained by every command line reaches `--help` first; the flag's handling must answer before any other action (D2 iii), and whether the same change makes `--help` answer as help is a scope call for the PM role — reported, not decided; inside the appetite only if the architect finds it free. R5: the external tools' beside-descriptions are a person writing in a machine shape — a document interaction with no pattern; in the second feature, noted for it. *unknowns* — U1: who the agent-user in the proof is and where its run is recorded; default: a fresh-context agent given "run the lint over the basis" with the skill loaded and no other source, its invocation and result recorded in the feature's delivery. U2: whether the skill's `description` is the answer's description verbatim or composed by the renderer; default: composed — the tool, its uses in one line, and its use-when — and tested by whether the harness offers the skill in U1's run. U3: whether the feature's delivery routes through the interaction-conformance-check process, which stands and nothing here names; default: this role runs the screen on the lint's skill (api) and the flag's answer (cli) at delivery, findings to the architect, undecidables to the corpus, the WCAG2ICT applicability record for the cli type written at the screen (the answer is text on standard output). U4: the vocabulary's words for the flag and the answer; default: the glossary's terms (framework tool, skill, gap) and the ADR's names, screened under D2 and entered as hypotheses. *evidence* — the initiative (v4); req-2026-09-06-tool-skills (v3); sess-2026-09-06-c, its evidence table; the experience principle set (v2); the core-task list (v4); the vocabulary (v3) and patterns (v2) records; the common (v3), api (v3), and cli (v3) experience guidelines; the interaction fitness set (v5) and the interaction-conformance-check process (stands); the working principle set (v11, `tools-through-skills`); the glossary (v23: interaction type, generated interface, framework tool, skill, gap); the role-offer type (v2); the initiative typedef (v11) and fitness set (v5); initiative-check (v9); the rendered skill .claude/skills/initiative-check/SKILL.md as the form an agent reads today; the architect's `--help` observation in this history's v3, taken as observed use; not read: the research report (research branch, not among this step's inputs — the architect's reading of its F1–F4 taken as the architect's) and the tools' source (the authority's direction). *Maker's self-check* against the initiative fitness set — scenario 5: §2 names no type, so the Then asks no usability evidence of §4; the verdict rendered there with reasons; the five parts each present here, none reading "none" but the two `record` fields, each with its reason; scenario 4: no interface form added to §1–3, which are untouched; scenario 7: 591 words after this attachment (528 + 63, counted by hand, no shell at this step), over 500 by 91, within the 20% variance the owner ruled at the init-role-decisions bet (its Document History v8) while the cap's split stands unruled. |
initiatives/init-flow-simplification.md:20:Problem: twenty agent runs and six human decisions stand between a bet and a build. Most are screens on records of decisions already taken. Outcome: after discovery and framing, a feature is authored, assigned, built, and verified with no human step and no check cycle; decisions are recorded after the feature, from what it needs; quality review is a sweep run on request whose findings become requests.
initiatives/init-flow-simplification.md:24:The authority, who frames and reads sweeps; every role, which makes and self-checks. Measure: agent runs from bet to verified build. Now: about 20. Target: 6 or fewer. Interaction types: command line.
initiatives/init-flow-simplification.md:47:| 2 | 2026-09-08 | update | Delivered: the flow runs from bet to build in six agent runs with no human step; measure met on init-run-measurement. |
guidance/feat-run-measurement-shopsystem-product.md:54:   which records each agent run's step and role and its start and end
guidance/feat-run-measurement-shopsystem-product.md:59:   two sources alone it writes one row per agent run naming step,
guidance/feat-run-measurement-shopsystem-product.md:62:   counts an agent run only for each step run by an agent or a human
initiatives/init-run-measurement.md:20:Problem: the cost of a run is known only when someone reads the transcripts by hand, as was done once for init-tool-skills. Outcome: every session close records, without a model, cost metrics for each agent run — step, role, minutes, context tokens, output tokens where the harness gives them, tool uses — beside the session record, so any run can be analysed on request.
initiatives/init-run-measurement.md:77:| 2 | 2026-09-08 | update | Feasibility and decomposition attached by the lead-solutions-architect role at initiative-check's attach-architecture step. The full offer, in the role-offer type's parts. *role* — lead-solutions-architect. *verdict* — feasible as framed, for a run the router moves; bounded elsewhere. Reasons: the outcome asks for mechanical data gathering only — no model, rows and nothing else, exactly the no-gos. Two of the four fields are already within reach through work this shop has already done: a run's context is recorded on its anchor at the run's end (feat-process-runner v9, the scenario "the run's context is recorded on the anchor at its end"), and the delivered router already reads the harness's own usage report at finer grain than that scenario requires — per resumed segment and per launched role, both on haiku and on fable (feat-process-runner's Document History v9: "the lane run 40 turns, cache-read 1.69M on haiku... the launched roles 1.15M on fable"). Minutes and tool uses need no model either: a runtime step reads timestamps and tool-invocation counts off the same anchor and transcript the router already reads, deterministically. Output tokens are the one field the framing itself already qualifies ("where the harness gives them"): the cost analysis that opened this whole initiative had to estimate them from characters, "the transcripts carry only a streaming stub" (req-2026-09-07-run-efficiency, §Evidence) — so a row that leaves this field blank is not a shortfall, it is the scoped answer. Not feasible without qualification: a session that no approved process definition moved through the router — most sessions run today, including the one that opened this check — carries no anchor with per-step values to read, so nothing mechanical exists yet to key a row on; "every session" is reachable only as the sibling init-process-runner's rollout reaches every session's work, and that initiative's own measure is not yet met (4.6M router context for one small change, feat-process-runner v9). *decisions* — D1, decision: "The one row per agent run is written to a new artifact kept beside the session record — never as an amendment to the session-record schema, which this shop does not own (`pkg:shopsystem-knowledge/session-record`, fetched through that package's own contract tool). The row is written by a runtime step, never an agent step: it reads the run's anchor, where the run passed through the router, and the harness's own usage report; a field the harness does not expose for a given run (most often output tokens) is left blank, never estimated and never computed by a model. The step's home is session-handoff-process's `collect` step, the process that already produces the session record this artifact sits beside; whether reconcile-and-close's own close and a review conversation's anchor need the same step is a separate question, not decided here." Under this role's guardrail right, the same right adr-2026-09-07-coordinator-role stands on; reversible — the artifact and the step are removed without changing what either process's typedef promises elsewhere; record: adr-2026-09-08-run-cost-artifact. D2, decision: "The initiative's own words scope the target to the session-record anchor — 'beside the session record' — not to a review record or a work item; a row for those two anchors, if wanted, is a separate request against reconcile-and-close and the review conversation's own anchor, not folded into this bet's appetite." Under this role's decomposition right (which activity carries which capability); record: adr-2026-09-08-run-cost-initiative-scope. *risks* — R1: output tokens and tool-use counts are confirmed available only by hand, from a full transcript read (the cost analysis this initiative answers did exactly that); if the harness's own usage report does not carry a per-tool-use count the way it carries per-turn cache-read tokens, that field is blank on every row until a transcript-parsing tool is built — enabler work, not this attach step's to commit. R2: most sessions today are not router-moved (this very check is one), so the "every session" target is coupled to a sibling initiative's rollout the authority has already bet on but not yet met; a bet here that assumes full coverage on day one overstates what ships. R3: two close processes exist (session-handoff, reconcile-and-close) and review conversations have no anchor process defined yet (this branch's own primer names the reconcile-side amendments as open); a step added to session-handoff alone leaves work and review conversations uncounted, which D2 scopes out rather than hides. R4: the new artifact must reference the session record's own frontmatter (id, created, authors) rather than restate it, or the two records drift apart under `single-source-of-truth`; the maker's discipline at build, flagged here so the build is checked against it. *unknowns* — U1: whether "minutes" means wall-clock, agent-minutes, or both; the cost analysis this initiative answers tracked both as distinct columns. Default: wall-clock only, read off the anchor's own timestamps, since agent-minutes needs summing turns the anchor does not yet keep separately. U2: the new artifact's exact name and location. Default: one file per session record, named to pair with it, in `sessions/`, the maker's choice at build. U3: whether "one row per agent run" counts a runtime step (no agent, no minutes of its own). Default: no — a row per agent or human step and per top-level router/lead-pm turn only. U4: whether review and work conversations are in the target's "every session." Default: no, per D2 — the framing's own words name the session record alone. *evidence* — this initiative (v1); the parent init-run-efficiency (v1) and its measure; req-2026-09-07-run-efficiency (v3), its cost table and the four places it names the cost sits, read in full; sess-2026-09-07-b, the session that framed this initiative and the sibling bet; basis/processes/session-handoff.md (v3) and basis/processes/reconcile-and-close.md (v4), both naming the router and producing, respectively, the session record and a work item's close; basis/glossary.md's `anchor`, `session record`, and `conversation type` entries — three conversation types, three anchors, one of which (review) names no process yet; feat-process-runner (v9) in full, its context-at-end scenario and its delivery history rows v7–v9, the harness's usage-report evidence at segment and role grain; adr-2026-09-07-coordinator-role (v3, checked), the guardrail this offer's right matches; basis/contexts/ — absent, confirming no Bounded Context exists on this branch. The feature repository swept in full — seven other features (feat-request-routing, feat-role-decisions, feat-roles-availability, feat-skills-availability, feat-tool-skills, feat-tool-skills-rest, feat-typedef-rendering) read for conflict: none names a session record, a cost row, or a close step; no conflict. Principles screen: `knowable-shape` — no first-class entity added, the new artifact is a lead-shop-held record, not a Bounded Context or a shop; `contracts-between-contexts` — not touched, no Bounded Context in scope; `actor-neutral-discipline` — the row records the same fields for a run whoever or whatever model tier moved it; `local-comprehension` — the artifact sits at the coordinating level beside the record it measures, readable without opening a Bounded Context; `bidirectional-conformance` — D1 and D2 are recorded here before any step is added to session-handoff-process, so the amendment is a design change first, not drift; `intent-provenance` — traces to req-2026-09-07-run-efficiency and sess-2026-09-07-b, both already recorded. Maker's evaluation against the role-offer type and the initiative typedef's §4–5: the four list parts each carry entries, no `none`; the verdict carries reasons; §4 rendered short with the designer's part left "not yet"; §5 attached with the flow "none"; `record: none` twice, each with its reason stated inline since this is the first pass; the offer names no Bounded Context's internals — basis/contexts/ does not exist on this branch. |
initiatives/init-run-measurement.md:80:| 4 | 2026-09-08 | update | The one revise, by the lead-pm at the initiative-check revise step (anchor lead-fresb, 01:04), in its own sections only: Appetite bounded — 2 hours per initiative; 50K context tokens; each no-go given its reason (analysis belongs in a separate orchestration step; this step records mechanically, model-based analysis out of scope); Framing's "one row per agent run" restated as "cost metrics for each agent run" and For whom's "a runtime step of session handoff" as "cost recording is incorporated into the session handoff step" — outcome language in place of structure. "Rows only" stands inside the first no-go, an exclusion naming what it excludes — the PM role's reading filed on the initiative fitness set's Document History 2026-09-04, the owner's ruling still open. The roles' attachments in §4–5 untouched: the designer's §4 quotes §2's former wording ("a runtime step of session handoff"), now stale against the revised §2 — disclosed here, that role's text to bring current, substance unchanged. Word count outside the history: 485. Every named finding repaired; the wobbly one repaired rather than held. |
features/feat-run-measurement.md:21:  for each agent run — step, role, minutes, context tokens, output
features/feat-run-measurement.md:35:- *a session close records a cost row for each agent run* — shopsystem-product (the lead shop)
features/feat-run-measurement.md:73:  for each agent run — step, role, minutes, context tokens, output
features/feat-run-measurement.md:80:  Scenario: a session close records a cost row for each agent run
features/feat-run-measurement.md:81:    Given a session whose run passed through the router, leaving an anchor that records each agent run's step and role
features/feat-run-measurement.md:83:    Then a cost row is written beside the session record for each agent run recorded on the anchor, naming its step, its role, its minutes, its context tokens, and its tool uses, none of them computed by a model
features/feat-run-measurement.md:87:    Given an agent run whose harness usage report does not expose one of the cost row's fields
features/feat-run-measurement.md:93:    Given an agent run whose start and end are recorded as timestamps on the anchor
features/feat-run-measurement.md:114:| The cost of a run known only by reading transcripts by hand | the framing (Problem) | Scenario: a session close records a cost row for each agent run |
features/feat-run-measurement.md:115:| Cost metrics computed or estimated by a model | the appetite's second no-go | Scenario: a session close records a cost row for each agent run; Scenario: a field the harness withholds is left blank |
features/feat-run-measurement.md:118:| Whether a runtime step counts as an agent run for a row | the architect's unknown U3 (initiative history v2) | Scenario: no cost row is written for a runtime step |
features/feat-run-measurement.md:129:| 1 | 2026-09-08 | update | Authored by the PO role alone at feature-authoring's draft step, from init-run-measurement's Framing and For whom sections (v5, planned); five scenarios, all owned by the lead shop per the Decomposition section ("None: no Bounded Context is touched — the new artifact, the runtime step, and any tooling change sit in the lead shop's own tree"); Interaction types stated as none per the For whom section's own words. Vocabulary drawn from the architect's and PM role's attachments (initiative history v2, v4, v5): the cost row's fields, the anchor and the harness's usage report feat-process-runner already reads, the cost artifact D1 places beside the session record (adr-2026-09-08-run-cost-artifact), wall-clock minutes (U1's default), and the session-record anchor D2 scopes the target to (adr-2026-09-08-run-cost-initiative-scope). The repository read in full — eight features (feat-request-routing, feat-role-decisions, feat-roles-availability, feat-skills-availability, feat-tool-skills, feat-tool-skills-rest, feat-typedef-rendering, feat-process-runner); feat-process-runner (v10) touched for its context-at-end scenario and its anchor and harness-usage-report vocabulary, its scenario text unchanged; no conflict — no other feature names a session record, a cost row, or a close step. `@hash:pending` on each of the five scenarios: no shell available to this authoring session; the repository convention (sha256 of the scenario's text, first twelve hex digits, as filled by the lead-pm for feat-request-routing and feat-typedef-rendering) applies before the check. Declined or held, with the reason in Edges: analysis of the recorded rows (the appetite's first no-go); a session not yet router-moved (the architect's feasibility finding); a review conversation's or a work item's close (D2); the new artifact's exact name and location (U2, the maker's choice at build). Self-check against the feature fitness set: 1 pass — each When one event, each Then one observable outcome on the anchor or the artifact beside the session record, no step naming a file path, a schema field, or a tool; 2 pass — an owning shop named for all five, no interaction type so no designer criteria due yet, the architect's own add-constraints step still to run; 3 pass on presence — both tags on all five, hashes disclosed pending; 4 pass — eleven rows from the framing, both no-gos, U1–U3, R1, R3, R4, and D1–D2, each covered by Scenario name or out of scope with a reason; 5 pass — none, with the reason quoted from the For whom section; 6 pass — who (the authority; the run-efficiency parent), what (a mechanical cost row per agent run, without a model), the outcome (any run analysed on request, in place of reading transcripts by hand). Feature id added to init-run-measurement's Features section. |
features/feat-run-measurement.md:132:| 4 | 2026-09-08 | update | Repairs made by the PO role against the PO output check's three named findings (all criteria named against the feature fitness set, none uncovered), read against feat-run-measurement v3. Fitness Scenario 7 (confident): the Contributors "Vocabulary" block's six entries — cost row, anchor, the harness's usage report, the cost artifact, wall-clock minutes, the session-record anchor — were none of the three permitted passage types (owning shop / criterion / constraint) and rode by name on no scenario. Repair: the block removed from Contributors, leaving the owning-shop list, the "none due" usability line, and the "none" constraints line only. No re-homing to the glossary was needed: each term's authoritative definition already sits at the source the block itself cited and that this feature's own Document History v1 already carries as the reasoning behind the scenarios drawn from them — cost row in the framing's own words; anchor in the glossary and feat-process-runner's context-at-end scenario; the harness's usage report in feat-process-runner's Document History v9; the cost artifact in D1 (adr-2026-09-08-run-cost-artifact); wall-clock minutes in U1 (init-run-measurement's Document History v2); the session-record anchor in D2 (adr-2026-09-08-run-cost-initiative-scope). Fitness Scenario 5 / framing (wobbly): the Interaction types section's "none" reason quoted the initiative's For whom section, a section the framing input named to the check does not carry. Repair: restated from the Framing section's own words alone — the outcome ("every session close records, without a model, cost metrics for each agent run ... beside the session record, so any run can be analysed on request") is stated entirely as a system state change, naming no command line, terminal, graphical or web screen, API or SDK, conversational or voice exchange, or generated document — so no core-task-list interaction type applies. No owner ruling was needed: the Framing section's own text supports the same "none" unaided, without reaching into the For whom section or any other role's domain. Fitness Scenario 1 (wobbly), the fifth scenario ("the cost artifact stands beside the session record without restating it"): its Then named "frontmatter" and "schema field," document-structure terms rather than an outcome observable in the running system. Repair: restated as "referencing the session record's own identifying fields rather than restating them, and the session record itself is left unchanged" — an outcome checkable by reading the two records, naming no field or schema. No ask was raised: all three repairs restate content already carried by the framing, the Contributors block's own citations, or the scenario's own subject matter, reaching neither the originator's intent, a scope question, nor another role's domain. Self-check against the feature fitness set: scenario 7 — the Contributors body now holds an owning shop, a "none due" usability line, and a "none" constraints line only, no reasoning passage; scenario 5 — the "none" reason traces to the Framing section's own words, not the For whom section; scenario 1 — the fifth scenario's Then names an outcome (a reference relation and an unchanged record), no implementation detail, and the other four scenarios re-read unchanged still pass; scenarios 2, 3, 4, and 6 unaffected by these repairs. `@hash:pending` left as is on all five scenarios — none was ever filled, per v1's note that no shell was available to that authoring session. Version bumped 3 → 4. |
features/feat-run-measurement.md:137:| 9 | 2026-09-08 | update | Delivered and verified by the lead-pm: write_cost_rows.py run on anchor lead-5wzgl writes sessions/sess-2026-09-07-b-cost.md, one row per agent step and router turn, blank where the report gave no figure. Six agent runs from bet to build under product-flow v7. |
guidance/feat-tool-skills-shopsystem-product.md:132:   agent run of item 5 recorded; and the delivery marked "hypothesis"
