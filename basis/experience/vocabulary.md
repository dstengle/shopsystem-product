---
type: experience-record
id: experience-vocabulary
record: vocabulary
owner: product-authority
status: draft
version: 7
created: 2026-08-26
updated: 2026-09-09
maintained-by: lead-product-designer
---

# Experience record: vocabulary

The product's one vocabulary — the word every interaction type uses
for each thing and action — with the platform mappings the
`consistent-not-uniform` principle allows. Read by the
[common experience guideline](../guidelines/experience-common.md)
rule 1 and the
[interaction fitness set](../fitness/interaction.fitness.md) scenario
1. An entry means: this is the word; an interaction using another is a
finding unless the mapping column records the platform's word for that
type. Glossary terms carry the glossary's meaning. Every entry is a
hypothesis until user research confirms the person-facing word; the
screen returns "undecidable: entry is a hypothesis" for it until then.

## Entries

| Term | Meaning | Platform mappings (type: word) | Source | Status |
|---|---|---|---|---|
| Bounded Context | see glossary | none recorded | glossary | hypothesis |
| shop | see glossary | none recorded | glossary | hypothesis |
| run | see glossary | none recorded | glossary | hypothesis |
| hold, resume, cancel | the actions that move an execution between its states (see glossary: hold) | none recorded | glossary | hypothesis |
| ask | see glossary | none recorded | glossary | hypothesis |
| clarify | see glossary | none recorded | glossary | hypothesis |
| acceptance scenario | see glossary | none recorded | glossary | hypothesis |
| feature | a Gherkin Feature: one capability from the user's or agent's view, with its acceptance scenarios | none recorded | feature typedef | hypothesis |
| decision record | the record of one product-level decision | none recorded | product-decision-record typedef | hypothesis |
| backlog | the ordered list of requirements within the framing | none recorded | backlog-order typedef | hypothesis |
| framing | see glossary | none recorded | glossary | hypothesis |
| contract | see glossary | none recorded | glossary | hypothesis |
| check | the screen of an output against its criteria and the decision on it | none recorded | po-output-check process | hypothesis |
| framework tool | see glossary | none recorded | glossary | hypothesis |
| skill | see glossary | api: the skill's frontmatter in the Agent Skills format the harness loads — `name` and `description`, the harness offering the skill on its `description` | glossary; adr-2026-09-07-tool-answer §3, the third consequence | hypothesis |
| gap | see glossary | none recorded | glossary | hypothesis |
| standard question | the one question every framework tool is asked about itself, the same for every tool from any shop; the tool's answer to it is the sole source of its skill | cli: `--describe`, the standard flag — a long option, answered on standard output with exit status 0 before any other action, the tool's other arguments ignored | adr-2026-09-07-tool-answer §2 (the flag's behaviour); feat-tool-skills, Vocabulary | hypothesis |
| answer | what a tool says about itself in reply to the standard question: its name, its description, and one use entry per use it supports; an answer only when the exit status is 0 and the output parses against the tool-description data type — anything else is "cannot answer" | cli: JSON on standard output; api: `tool-description`, the [data type](../types/tool-description.md) (v1) — the versioned contract a tool answers to; `no-answer`, the failure code a tool reports when a tool it asks cannot answer | adr-2026-09-07-tool-answer §2 (the answer's shape; the contract artifact; the JSON bound); tool-description data type v1 (the code) | hypothesis |
| name (of a tool) | the tool's identifier, unique among the product's tools, matching `^[a-zA-Z0-9_-]{1,64}$` | api: `name` — the field Anthropic's tool definition and MCP give it; the skill's `name` | adr-2026-09-07-tool-answer §2 | hypothesis |
| name (of a use) | the use's name for the caller — what the skill calls the use, distinct from its invocation; unique within the tool's answer, in the same form as the tool's name | api: `name` in the use entry — the data type's field (v1); in the skill, the use's heading and its listing in the skill's `description` | tool-description data type v1; feat-tool-skills, criterion (c) (the description names the uses) | hypothesis |
| description | what the tool does and when to use it, in one or two sentences; in the skill, the field the harness offers the skill on, so it names the tool and its uses | api: `description` — Anthropic's tool definition and MCP; the skill's `description`, composed by the producer from the answer's description, the tool's path, and the uses' names | adr-2026-09-07-tool-answer §2; feat-tool-skills, criterion (c) | hypothesis |
| use | one thing a tool can be asked to do; the answer decides the list | api: `uses`, a list — the identifier the data type (v1) fixes, as proposed | feat-tool-skills, Vocabulary; tool-description data type v1 | hypothesis |
| use entry | a use's entry in the answer and in the skill: what it does, what it takes, what it returns, how it fails, and — in the skill — the exact invocation; each part present as itself, none read off another | api: the tool-skill use entry pattern (patterns record) | feat-tool-skills, Vocabulary; init-tool-skills, Document History v5, the designer role's first decision entry | hypothesis |
| what it does | the use's effect, stated as behaviour, including what it does not do | api: the use's `description` | adr-2026-09-07-tool-answer §2; api guideline rule 2 | hypothesis |
| what it takes | the use's command line and each input, named for the caller, with an omitted input's treatment stated | cli: the command line — inputs as flags, at most one positional; api: `input_schema`, a JSON Schema object over the arguments — Anthropic's field (MCP: `inputSchema`) | adr-2026-09-07-tool-answer §2; api guideline rules 1 and 2; cli guideline rule 2 | hypothesis |
| input | one thing a use takes, named for the caller: what it means, and what happens when it is omitted — the default taken, or that it is required | cli: a flag (`--name <value>`) or the one positional; api: a property of `input_schema`, `required` listing those with no default | adr-2026-09-07-tool-answer §2; api guideline rule 1; cli guideline rule 2 | hypothesis |
| what it returns | what the use writes to standard output on success, and its form: a JSON Schema where the output is structured, a stated text form where it is not | api: `returns`, an object the data type (v1) fixes in place of the bare `output_schema` proposed — `form` (one of `json`, `yaml`, `text`); `output_schema`, a JSON Schema present when the form is `json` or `yaml` (the snake-case form of MCP's `outputSchema`); `text`, present when the form is `text` — the statement of the text form, never the output itself; in the skill, "Returns (form):" | adr-2026-09-07-tool-answer §2; tool-description data type v1 (the mapping) | hypothesis |
| how it fails | the use's failures, one entry each: the failure code, the exit status, the condition that produces it, and the next step | cli: the EXIT STATUS list of man-pages(7); api: `failures`, a list — the identifier the data type (v1) fixes, as proposed | adr-2026-09-07-tool-answer §2; api guideline rule 3; common guideline rule 3; tool-description data type v1 | hypothesis |
| failure code | a failure's identifier, the same on every run that fails the same way, from the closed set the tool-description data type names; it stands beside the condition and the next step, never in place of them | api: `code`, the api guideline's error shape; the codes themselves are hyphenated words (`check-failed`, `no-answer`), the form of the shop's finding tokens (`unrecognized`, `tool-diverged`), while the shape's field names are snake_case as the platform's are (`input_schema`) — one convention per identifier kind, `consistent-not-uniform`; cli: written in the message on standard error beside the exit status — the code is not the exit status | adr-2026-09-07-tool-answer §2; feat-tool-skills, Vocabulary ("a stable code"); api guideline rule 3; tool-description data type v1 (the set and its form) | hypothesis |
| exit status | the number the tool's process ends with: 0 for an answer and for a use that succeeds; non-zero and distinct from success for a failure | cli: the exit status (clig.dev "Errors"; man-pages(7) EXIT STATUS); api: `exit_status` — the identifier the data type (v1) fixes, as proposed | adr-2026-09-07-tool-answer §2; cli guideline rule 4; tool-description data type v1 | hypothesis |
| condition | what produces a failure, said in the vocabulary — the "what happened" of the error pattern | api: `condition` in the answer — the identifier the data type (v1) fixes, as proposed; in a tool's own error output, `message`, the api guideline's error shape | adr-2026-09-07-tool-answer §2; common guideline rule 3; tool-description data type v1 | hypothesis |
| next step | what the caller does after the failure, one step | api: `next`, the api guideline's error shape; the data type's field (v1) | adr-2026-09-07-tool-answer §2; api guideline rule 3; common guideline rule 3 | hypothesis |
| exact invocation | the one command line stated for a use — the first command the agent issues for it, copyable as written, an input's placeholder in angle brackets | cli: the command line; api: `invocation` in the use entry — a field of the answer, which the data type (v1) places there so the skill carries it verbatim; proposed for the skill alone, recorded here as the answer's too | feat-tool-skills, Vocabulary ("the first invocation"); init-tool-skills, Document History v5, the designer role's first decision entry; tool-description data type v1 (the mapping) | hypothesis |
| produced from | the skill is a rendering of the answer: never edited by hand, never the source; the tool it was produced from named in the skill | api: the skill's `source` (the tool asked) and `source-digest` (the digest); beside them the stamps every rendering carries — `generated`, `generated-by` (the producer), `derived-from` (the skill's name) | adr-2026-09-07-tool-answer §2 (the digest gate); feat-tool-skills, Vocabulary; the lint's skill as produced (source-digest c523a32fcb0f) | hypothesis |
| digest | the stamp the skill carries over the answer it was produced from: `sha256:` and twelve hex digits over the answer's bytes as written; a fresh answer whose digest differs is drift, resolved toward the answer | api: `source-digest` | adr-2026-09-07-tool-answer §2 (the digest gate); feat-tool-skills, Document History v5, ruling (i) | hypothesis |
| producer | the framework tool that produces a tool's skill from its answer and from nothing else: `basis/tools/compile_tool.py`; where a record says "the renderer" or "the compiler" of a tool skill it names this tool | api: the skill's `generated-by` | feat-tool-skills, Document History v5, ruling (i); adr-2026-09-07-tool-answer §3, the third consequence | hypothesis |
| beside-description | the stand-in for a tool that cannot answer: a description in the answer's shape, written beside the tool at the home the data type names, carrying which tool it stands beside and who owns that tool; retired when the tool answers | api: a JSON file at `basis/tools/descriptions/<name>.json`, the data type's two fields (v1) — `stands_beside` (the path of the tool it stands for) and `tool_owner` (the shop that owns that tool, not the shop that wrote the file — the designer's finding, taken in the data type's v2); the skill's `source` names the file, not the tool | adr-2026-09-07-tool-answer §2 (the stand-in description); tool-description data type v2 | hypothesis |

## Checks

[Common experience guideline](../guidelines/experience-common.md)
rule 1; [interaction fitness set](../fitness/interaction.fitness.md)
scenario 1.

## Document History

| Version | Date | Kind | Entry |
|---|---|---|---|
| 1 | 2026-08-26 | update | Seeded from the glossary and the approved definitions. |
| 1 | 2026-08-26 | review | Screened: findings — invented CLI mappings for a command line that does not exist; meanings drifting from the glossary; "scenario" not the defined term; a person-facing column promised and absent. |
| 2 | 2026-08-26 | update | Repairs: mappings removed; glossary terms say "see glossary"; acceptance scenario used; every entry marked hypothesis pending user research. |
| 2 | 2026-08-26 | review | Re-screened: clean. |
| 2 | 2026-08-26 | review | Re-screened (round 3): clean. |
| 3 | 2026-08-28 | update | Owner decision: acceptance-scenarios re-formed as feature (product-level, scenarios assigned per Bounded Context by tag); the brief retired — shops receive their assigned scenarios. |
| 4 | 2026-09-07 | update | The tool terms entered as hypotheses by the lead-product-designer role — its own resulting action, recorded at init-tool-skills (Document History v5: the second decision entry's part (i), the second risk, the fourth unknown) and at feat-tool-skills (v2: the Contributors section's "the vocabulary holds no tool term" and the Edges row on the skill's names being undecidable until the terms are entered), under adr-2026-09-07-tool-answer (v3, checked: §2 the answer's shape, the contract artifact, the digest gate, the stand-in description). Twenty-one entries: the glossary's framework tool, skill, and gap ("see glossary", mappings added); the standard question, cli mapping `--describe`; the answer and its parts as the ADR fixes them — name, description, use, use entry, what it does, what it takes, input, what it returns, how it fails, failure code, exit status, condition, next step — and the exact invocation, produced from, and beside-description. The api mapping carries the field name where the ADR or the platform form fixes it (`name`, `description`, `input_schema`, `code`, `next`, the skill's `source` and `source-digest`); where neither does, an identifier proposed to the solutions architect role for the tool-description data type — `uses`, `output_schema`, `failures`, `exit_status`, `condition`, `invocation` — a recommendation on the contract's ergonomics, the data type's own choice recorded here as the mapping if it differs. The closed set of failure codes is the data type's, not entered here. Every entry a hypothesis until an agent's use of a tool skill is observed (feat-tool-skills, its last three scenarios); the feature and the initiative untouched. Maker's self-check against the experience-record typedef (v3) derived checklist, as define-good-up-front asks: `record` vocabulary and `maintained-by` present; the columns term · meaning · platform mappings · source · status unchanged; every entry carries a source that names a definition or "design judgment" and the `hypothesis` status; glossary terms read "see glossary"; the types named use the closed tokens `cli` and `api`; the Checks section unchanged. The lint could not be run from this session (no shell); its mechanical checks applied by hand — frontmatter and version present, Document History last, no relative link added (the data type's home written as a path, not a link, since the file is not yet made), no banned term, no numbered-decision reference. |
| 5 | 2026-09-07 | update | The tool-description data type's field names (v1, draft) screened by the lead-product-designer role against this record and the patterns record (v4) at feat-tool-skills' delivery (v7; v8, the lead-pm's verification and its second ruling: the type's approval follows this screen) — the action the initiative's Document History (v5, the second risk) and the data type's own field notes assign to this role — and entered as the terms' identifiers. Screened by the api guideline's rule 1 (each name read without its implementation: can a caller say what it is from the name and this record alone) and rule 3 (the error shape): every identifier the type fixes where this record proposed one is the proposed one — `uses`, `failures`, `exit_status`, `condition`, `invocation`; where the ADR or the platform fixes one, that one — `name`, `description`, `input_schema`, `code`, `next`. The three the type names as differing, mapped here: `returns`, an object wrapping `output_schema` beside `form` and `text` (the "what it returns" entry, with `text` recorded as the statement of the form, never the output itself — the one reading a bare `text` does not give on its own); `invocation` as a field of the answer as well as the skill (the "exact invocation" entry, its meaning no longer "the skill states" alone); a use's own `name` (a new entry, "name (of a use)", distinct from the tool's). Also entered: the beside-description's two fields, `stands_beside` and `owner`, and its home (the "beside-description" entry — `owner` read as the tool's owner, the ambiguity against the corpus's `owner` frontmatter recorded as a finding in the data type's history for its owner, the field's meaning fixed here whichever way that resolves); `no-answer` as the failure code for "cannot answer" (the "answer" entry — two words for one condition from two sides, the verdict on a tool and the failure a caller meets, mapped rather than merged); the failure codes' hyphenated form beside the field names' snake_case, one convention per identifier kind under `consistent-not-uniform` (the "failure code" entry; the set itself stays the type's, referenced not copied); the skill's `description` as composed by the producer (the "description" entry — the initiative's v5, the second unknown's default, observed met: the harness offered the skill); the rendering stamps beside `source-digest` (the "produced from" entry, from the lint's skill as produced). Two terms entered on feat-tool-skills' Document History v5 ruling (i), which assigned them to this role at the delivery screen: "digest" and "producer" — the latter the one word for what the records also call the renderer or the compiler of a tool skill, `compile_tool.py`. Names read in the lint's skill (digest c523a32fcb0f) as an agent meets them: every heading, label, and table column is an entry's term or its recorded api mapping. Every entry stays a hypothesis: one agent's observed run (feat-tool-skills v7) is one run of measured task completion on four uses and two failures — evidence for the skill's names in use, not yet for the person-facing words. The feature, the initiative, and the data type's body untouched. Maker's self-check against the experience-record typedef (v3) derived checklist, as define-good-up-front asks: `record` vocabulary and `maintained-by` present; the five columns unchanged; every entry carries a source naming a definition, an observed rendering, or "design judgment", and the `hypothesis` status; the types named use the closed tokens `cli` and `api`; the Checks section unchanged. The lint could not be run from this session (no shell); its mechanical checks applied by hand — frontmatter `version` 5, Document History the last section, the one link added resolves (`../types/tool-description.md`, now in the tree), no banned term, no numbered-decision reference. |
| 6 | 2026-09-07 | update | The beside-description entry follows the data type's v2: the identifier is `tool_owner`, the designer's finding taken before the type's approval. |
| 7 | 2026-09-09 | update | `run` propagated to `execution` as the noun for a process instance, under req-2026-09-08-definition-vs-instance / feat-execution-vocabulary (shopsystem-product): mechanical, determiner-adjacent occurrences only (`a/the/this/one/another/each/no/any run(s)`); `run-by`, `run` as a schema field or step key, and compound/heading uses (e.g. `run-cost`, `run list`, `Run lifecycle`) left unchanged, that residue disclosed as not done in this pass. Self-check against define-good-up-front: diffed against the file's pre-edit text; no requirement, field name, or heading changed. Made by the lead-solutions-architect role. |
