message_type: request_bugfix
work_id: lead-015
description: "Tighten shop-msg read outbox: pin the schema-validation-failure path.\
  \ The Reviewer of lead-014 (clarify outbox) flagged that cli.py implements ValidationError\
  \ handling (try/except \u2192 stderr message + exit 1) but no scenario pins the\
  \ contract. A future simplification could remove the try/except and let an unhandled\
  \ traceback escape; all 3 current read_outbox scenarios would still pass. This bugfix\
  \ is ADDITIVE \u2014 the existing 20 scenarios must continue to pass."
scenarios:
- hash: c039ab184dd1bbb8
  tags:
  - '@scenario_hash:c039ab184dd1bbb8'
  - '@bc:shop-msg'
  gherkin: "Feature: shop-msg read outbox \u2014 schema validation tightening\n\n\
    \  @scenario_hash:c039ab184dd1bbb8 @bc:shop-msg\n  Scenario: Read fails with a\
    \ stderr message when the outbox file fails schema validation\n    Given an empty\
    \ BC at a temporary path\n    And the BC's outbox already contains a file named\
    \ \"lead-099-clarify.yaml\" with content that is valid YAML but does not match\
    \ the BCResponse schema\n    When I run shop-msg read outbox with work-id \"lead-099\"\
    \n    Then the command exits non-zero\n    And stderr explains schema validation\
    \ failed\n"
