message_type: request_maintenance
work_id: lead-003
description: Add a `from_fahrenheit` classmethod to the Temperature class that constructs
  a Temperature from a Fahrenheit value using the formula `celsius = (fahrenheit -
  32) * 5 / 9`.
acceptance_criteria:
- Temperature.from_fahrenheit(32).to_celsius() == 0
- Temperature.from_fahrenheit(212).to_celsius() == 100
- All existing tests still pass.
file_hints:
- src/temperature.py
- tests/test_temperature.py
