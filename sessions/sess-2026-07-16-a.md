---
type: session-record
id: sess-2026-07-16-a
title: Legacy corpus migration, then the full knowledge/schema precondition chain
status: closed
created: 2026-07-16
updated: 2026-07-16
authors: [dstengle, "Claude (acting lead-pm)"]
description: Deciding session reopening cand-001/PDR-032's legacy-corpus-migration exclusion; converges on a PDR draft, then a direct ADR-048 spot-check widens into a full precondition-chain audit that reframes and sequences the PDR as a downstream phase.
mode: deciding
produced: [intent-006, cand-004, pdr-034, intent-007, cand-005]
revised: [cand-005]
beads: []
---

# sess-2026-07-16-a — Legacy corpus migration, then the precondition chain

## Outcome

The product authority, on learning that the overnight corpus-validation
pass (brief-019/`lead-5msa9`) found ~97 legacy `adr/`/`pdr/`/`briefs/`
files carrying no frontmatter — including files authored the same week —
did not recall endorsing a permanent exclusion of that corpus from the
typed-schema system `cand-001`/PDR-032 introduced, and observed the split
undermines the schema work's own purpose (progressive-disclosure tooling
needs one consistent corpus to index).

Recorded as `intent-006` (verbatim anchor captured), shaped directly into
`cand-004` given the direction was already convergent, then run through
the deciding conversation on the one genuine open tradeoff — migration
sequencing. Framed three options at parity (leave excluded / single-pass
/ phased gate-verified batches); the product authority chose phased
gate-verified batches, weighing lower fidelity risk on ~90 files of
variably-formatted legacy content against slower full closure.

Converged on `pdr/034`, drafted — not yet ratified. It supersedes only
PDR-032's legacy-migration appetite line, not the six-type schema itself,
and explicitly routes the batching-grouping and migration-mechanism
questions to an Architect feasibility probe rather than deciding them at
the PM altitude.

Before dispatching `pdr/034`, the product authority asked a direct
verification question — does `adr/048-...md` (visibly missing
frontmatter) actually pass `shop-knowledge validate`? It correctly
doesn't. But checking it prompted checking the system around it, and the
same session's own newly-produced artifacts (`pdr/034`, `cand-004`,
`intent-006`, plus this session record itself) failed the same validator
immediately on first run — the same shape as `lead-6n4j6`'s already-filed
finding, now confirmed against five independently-authored instances
including brand-new ones. Direct inspection of `shop-knowledge template
<type>` output, the poured `.claude/skills/*` (zero reference
`shop-knowledge` at all), the installed `shop-templates` version
(`0.52.6`, predating `lead-5msa9.2`'s unreleased fix), this repo's pour
history (last full re-render `693d113`, 2026-07-10, `v0.51.0`), and the
absence of any CI/`bin/doctor` enforcement surfaced ten linked
preconditions, of which only one (the `shop-knowledge` CLI itself)
actually holds.

Recorded as `intent-007`, shaped directly into `cand-005` — five phases
in dependency order (typedef correctness → release+repour → minimal
enforcement → build the actual coherence gate → migrate the legacy
corpus). `cand-004`/`pdr/034` are not superseded — their content stands
unchanged — but both are now explicitly parked as `cand-005`'s Phase 5,
since dispatching the legacy migration ahead of a working typedef/gate
would have added an eleventh broken link instead of closing the chain.

`cand-005` committed same session: full-chain appetite ("fund it all"),
not foundation-first. Phase 1's direction (fix the typedef vs.
restructure practice) stays an Architect feasibility call, not resolved
by the ratification — routed onward as the immediate next dispatch.

## Outcome, continued — cand-005 phases 1-4, Dean Peters skills, shutdown prep

**cand-005 phases 1-3: dispatched, landed, and independently verified —
not rubber-stamped.** Phase 1 (typedef correctness): Architect feasibility
work found `intent-record`/`session-record` had ZERO governing scenarios
for body structure (never formally specified, not merely drifted);
`candidate` had one partial scenario whose own status-enum assumption was
itself wrong; `pdr` needed no fix. PO authored 19 scenarios; a first
Architect verification pass correctly BLOCKED dispatch on a real ADR-064
D2 violation (retirement missing its provenance comment) before it could
ship; fixed directly, re-verified, dispatched as `lead-x53ez`. `work_done`
landed and was verified *behaviorally* (installed the new
`shopsystem-knowledge` build, confirmed `shop-knowledge template`/
`validate` actually emit/check the corrected structure against real
files, not just that hashes matched). Phase 2 (release+repour): cut
`shop-templates v0.53.0` carrying three pending fixes, upgraded this
host, ran a FULL repour (not another single-file edit) — verified via
concrete grep-count before/after (0→5 skills wired to `shop-knowledge`).
Phase 3 (minimal enforcement): found `bin/doctor` already existed as
unpoured package data, poured it byte-verified; built and tested a real
pre-commit hook that blocks new non-conforming artifacts while only
warning on the ~125 pre-existing legacy failures (a blanket block would
have made the repo unusable before Phase 5).

**cand-005 Phase 4: dispatched, then correctly REJECTED on work_done, not
closed.** PO brief authoring found the coherence-gate check logic
(PDR-032 rules 4-8) was already fully pinned but undispatched after a
prior mis-scoped attempt (`lead-5oih`); real remaining scope was wiring
it into a lead-installable CLI. Dispatched as `lead-iohr` (34 scenarios,
verified). `work_done` came back with clean hashes but the CLI was
behaviorally broken against this repo's REAL corpus (not the BC's own
test fixtures): a `pdrs`/`adrs` vs. `pdr`/`adr` directory-key mismatch
meant `PDR-034`'s `supersedes` claim was never evaluated at all, and a
raw-filename-stem legacy-id derivation meant `current-state.md`'s
`incorporates` claim got the wrong verdict. Rejected, not closed; filed
`lead-cea24` (P1, full repro+fix shape) — **not yet dispatched as of
session close.** Phase 5 (`cand-004`/`pdr-034`, legacy migration) remains
blocked pending Phase 4's real landing.

**Separately, same session: PM technique skills restored under a direct
license grant.** The product authority obtained explicit permission
directly from Dean Peters (rights holder) to use the 20
`deanpeters/Product-Manager-Skills`-derived PM technique skills reverted
2026-07-10 for CC-BY-NC-SA/MIT incompatibility, conditioned on
attribution. Recorded as `adr/066` (resolves within ADR-061 D2 — an
attribution-only grant is CC-BY-equivalent, already MIT-ingestible — not
by overturning D1's general reasoning). All 20 restored with upgraded
attribution, cross-checked against `cand-005`'s own schema fixes (found
and fixed 2 real drifts — stale "open questions" references corrected to
the actual pinned "Open threads" section name). One was exercised for
real (`incoming-request-advisor`, decoding the product authority's own
"let's exercise the skills / start optimizing" request) — that decode
opened a genuine discovery-dialogue on framework self-optimization which
is **queued, not started**: first pass at scoping it (narrowing to "the
PM technique skills themselves") was explicitly corrected by the product
authority to "open discovery" instead, then deprioritized in favor of
continuing `cand-005` — still open for a future session.

**Session-close / shutdown prep:** product authority requested full
commit+push across the lead and fleet ahead of a host reboot, and this
session to be clearable and resumable after. Lead repo: clean, pushed
(includes committing the accumulated pre-pause OpenRouter/fabro-thread
work — ADR-064, briefs 020-022, `cand-003`, two runbooks — which multiple
agents tonight had correctly left untouched out of respect for the
"pause fabro work" instruction, now safe to commit since it was
completed, verified work, not resuming the pause). `bd dolt push`
succeeded. Fleet-wide git/bd/mailbox audit dispatched — see bd memory
`handoff-2026-07-17-pre-reboot` for its outcome and the full state
snapshot for the next session to resume from.

## Open threads

- **`lead-cea24`** (P1) — coherence-gate CLI's two real bugs (directory-
  key mismatch, legacy-id derivation) — fix not yet dispatched. Next
  action once resumed: dispatch `request_bugfix` to `shopsystem-
  knowledge`, re-verify behaviorally against this repo's real corpus
  again (not just the BC's own fixtures) before accepting.
- `cand-005` Phase 5 (`cand-004`/`pdr-034` legacy migration) stays blocked
  until Phase 4 genuinely lands.
- The fabro/OpenRouter thread (brief-017, `lead-yekmk` live-proof retry)
  remains paused pending explicit product-authority direction to resume
  — not touched by any of tonight's cand-005/Dean-Peters/shutdown-prep
  work.
- The framework-self-optimization discovery-dialogue (opened by
  `incoming-request-advisor`'s decode, scope corrected to "open
  discovery" by the product authority) is queued, not started.
- `lead-6n4j6` (the original, narrower typedef-vs-practice finding) was
  folded into `intent-007`/`cand-005` with sharper evidence; updated in
  place, not duplicated.
- `lead-c46ug` (P3) — minor process note from Phase 4's mechanism_observation
  (future dispatches could scope to net-new scenario surface only,
  skipping re-bundling already-pinned adoption scenarios) — not acted on.
