GAP A (create-bc deterministic beads provisioning, lead-3mez) — the BC-standup beads-tracker provisioning exec must carry a non-empty GH_TOKEN placeholder so `gh repo create` reaches the agent-vault proxy.

FIX: the docker-exec at bc_launcher/controller.py:231 that runs `gh repo create <owner>/<bc>-beads --private --add-readme` must set a non-empty GH_TOKEN placeholder in that exec's environment, so gh authenticates through the already-wired agent-vault proxy (the broker substitutes the real GITHUB_TOKEN) instead of exiting non-zero with a "gh auth login" / "populate GH_TOKEN" error. Neither --env-file nor host-env GH_TOKEN reaches this exec today; the placeholder must be set on the exec itself.

EMPIRICAL EVIDENCE (contract/artifact surface, ADR-018 D1): during David's create-bc standup of shopsystem-knowledge under fabro (2026-07-07 — a BC-reported run), the standup computed the correct target dstengle/shopsystem-knowledge-beads yet `gh repo create` FAILED exit 4, and dstengle/shopsystem-knowledge-beads returned 404 (repo never created). PROVEN: re-running the exact in-container script with GH_TOKEN=dummy created the repo successfully — the proxy (HTTPS_PROXY, broker CA, AGENT_VAULT_*) is fully wired; gh only lacks a placeholder token. This is a BC-side demonstration surfaced to the lead, not a lead-side execution.

PRE-STATE / VEHICLE (request_bugfix, additive tightening): the repo-creation behavior already EXISTS and is pinned in the lead-held features/ at features/shopsystem-bc-launcher/bc_standup_creates_beads_tracker_repo.feature @scenario_hash:90caf5523e7d5ce0 — but that pin explicitly hedges "gh/docker may be unavailable in-test" and does NOT pin the credential wiring. This scenario (c1abb192dd2a5eae) tightens that unpinned credential-wiring aspect. SCOPE IS ADDITIVE: @scenario_hash:90caf5523e7d5ce0 continues to hold and is NOT retired or superseded; this is a distinct new pin on the provisioning-exec env surface. Fidelity binds to the executable provisioning-exec surface (structural inspection of the controller.py:231 docker-exec env), NOT a live gh/GitHub call.

IMPORTANT: file the bd BDD-plan (umbrella + RED sub-issue) BEFORE integrating (lead-yj3o).
