GAP C (create-bc deterministic beads provisioning, lead-pqlx) — the BC scaffold's `bd init` must pass `--prefix` and COMMIT a definite issue_prefix into the .beads registry, so a newly stood-up BC's tracker is healable (the session-start health gate's adopt branch fires instead of "unhealable").

FIX: the shop-templates bootstrap `bd init` invocation passes a `--prefix <prefix>` argument that sets a definite issue_prefix, and the committed .beads registry NAMES that issue_prefix (rather than "Exported 0 issues" / no issue_prefix). Then `bd create` in the stood-up BC exits zero yielding a prefixed id, and the session-start work-tracker health gate can ADOPT the committed issue_prefix rather than classifying the tracker "unhealable".

EMPIRICAL EVIDENCE (contract/artifact surface, ADR-018 D1): during David's create-bc standup of shopsystem-knowledge under fabro (2026-07-07 — a BC-reported run), `bd create` failed "database not initialized: issue_prefix config is missing". ROOT: bootstrap's `bd init` does not pass --prefix, so the committed .beads/issues.jsonl carries no issue_prefix ("Exported 0 issues"). The session-start work-tracker HEALTH GATE — identical in tmux (shop_templates/templates/claude/bc.md) and fabro (bc_launcher/assets/fabro-def/nodes/bc-router.md 'health' node, lines 58-85) — heals ONLY by ADOPTING a committed issue_prefix "taken from the committed registry, NOT derived from the BC name"; with none committed it hits "block (unhealable): empty working set + no issue_prefix + committed registry names no prefix to adopt" and halts, so a truly-NEW BC never begins its role loop. (Existing tmux BCs work only because their prefixed registries already exist.)

PRE-STATE / VEHICLE (request_bugfix, additive tightening): the scaffold already runs `bd init` (pinned features/shopsystem-templates/bootstrap_bd_init_side_effects.feature @scenario_hash:31a044e7d2eceaf4, which pins --skip-agents, NOT --prefix). The --prefix + committed-prefix aspect is unpinned existing behavior. This scenario (bf4caf0a470dcaa3) tightens it. Distinct also from features/shopsystem-templates/bootstrap_beads_remote_and_prefix.feature @scenario_hash:0636fba2c1445f9f, which checks the LIVE `bd config get issue-prefix` for shop-type lead, not the committed registry a fresh BC ships. SCOPE IS ADDITIVE: @scenario_hash:31a044e7d2eceaf4 and 0636fba2c1445f9f both continue to hold and are NOT retired. Fidelity binds to the executable scaffold provisioning (`bd init --prefix`) and the committed .beads registry content, read against the health-gate node body's adopt/unhealable decision table, NOT a live container run.

END-TO-END ACCEPTANCE BAR (reconciliation gate, NOT a separately-dispatched pin): features/shopsystem-templates/end_to_end_healthy_tracker_after_standup.feature @scenario_hash:195ff0c3d6a61bfe is the cross-gap integration acceptance spanning GAP A (lead-3mez, bc-launcher — tracker repo exists), GAP B (lead-r34c, bc-launcher — functional remote resolves to <owner>/<bc>-beads), and GAP C (this dispatch). Its healthy-on-first-pass probe spine (`bd create --prefix` ; `bd ready` ; test `bd dolt push`) can only go green once ALL THREE land, and two of the three are owned by shopsystem-bc-launcher — so it is NOT a scenario shopsystem-templates can pin/green on its own. Treat 195ff0c3d6a61bfe as the LEAD-side reconciliation acceptance test: the lead verifies it once GAP A + GAP B + GAP C are all reconciled. It is carried here for context, not as a --scenario-file in this dispatch.

IMPORTANT: file the bd BDD-plan (umbrella + RED sub-issue) BEFORE integrating (lead-yj3o).
