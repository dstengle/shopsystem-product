GAP B (create-bc deterministic beads provisioning, lead-r34c) — the in-container BC-standup must write the RESOLVED owner into the functional bd dolt remote so `bd bootstrap` targets <owner>/<bc>-beads, not ORIGIN_OWNER/<bc>-beads.

FIX: the bc-launcher standup resolves ORIGIN_OWNER -> derived GitHub owner in the in-container .beads config + functional bd dolt remote (the one `bd dolt remote list` reports and `bd bootstrap` clones from) BEFORE `bd bootstrap` runs. By the time in-container `bd bootstrap` executes, NO literal ORIGIN_OWNER segment survives in the functional remote; its owner segment equals the derived owner so the clone target is <owner>/<bc>-beads.

EMPIRICAL EVIDENCE (contract/artifact surface, ADR-018 D1): during David's create-bc standup of shopsystem-knowledge under fabro (2026-07-07 — a BC-reported run), in-container `bd bootstrap` FAILED "dolt clone git+https://github.com/ORIGIN_OWNER/shopsystem-knowledge-beads.git ... Repository not found". The scaffold was pushed with the ORIGIN_OWNER placeholder (correct at scaffold time), and the standup resolved the owner to dstengle for the gh-create step (it printed dstengle/shopsystem-knowledge-beads) but did NOT write that resolved owner back into the in-container .beads config, so bd bootstrap cloned the stale placeholder URL.

DECOMPOSITION CALL (Architect, finalized): the missing writeback is IN-CONTAINER, after scaffold — this is the shopsystem-bc-launcher standup's job, NOT shop-templates'. The scaffold-side pins already PASS yet the placeholder still reached launch: features/shopsystem-templates/bootstrap_bc_beads_remote_owner_substitution.feature @scenario_hash:ef4f4d86d3e4d153 (config.yaml sync.remote text) and features/shopsystem-templates/bootstrap_bc_functional_bd_dolt_remote.feature @scenario_hash:8db8399c92702704 (functional remote at bootstrap time) both hold. The create-bc path runs no footing reconcile (features/shopsystem-templates/footing_host_port_and_beads_sync.feature @scenario_hash:c1b769fb49c6ebfb is the LEAD-only fill), so the placeholder survives to launch — which is why this pins the bc-launcher standup, not templates.

PRE-STATE / VEHICLE (request_bugfix, additive tightening): standup already provisions the tracker and runs bd bootstrap (pinned 90caf5523e7d5ce0). The runtime owner-writeback aspect is unpinned existing behavior. This scenario (8ca9508bd7f5fecf) tightens it. SCOPE IS ADDITIVE: @scenario_hash:ef4f4d86d3e4d153, 8db8399c92702704, c1b769fb49c6ebfb, and 90caf5523e7d5ce0 all continue to hold and are NOT retired — this is a distinct runtime-writeback pin. Fidelity binds to the executable standup provisioning surface plus the in-container config observed via `bd dolt remote list` / the `bd bootstrap` clone target, NOT a live GitHub clone.

IMPORTANT: file the bd BDD-plan (umbrella + RED sub-issue) BEFORE integrating (lead-yj3o).
